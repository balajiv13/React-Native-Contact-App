const contacts = (state:any, {type, payload}: any)=>{

    switch (type) {
        case 'GET_CONTACTS':
            return state;
    
        default:
            return state;
    }
}

export default contacts